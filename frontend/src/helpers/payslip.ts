type Entry = { employee: { user?: { name?: string; email?: string } }; month: number; year: number; grossPay: number; deductions: number; netPay: number; payableDays: number; workingDays: number }
const escapeHtml = (value: unknown) => String(value ?? '').replace(/[&<>'"]/g, (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' })[character] || character)
export function generatePayslipPdf(entry: Entry) { const page = window.open('', '_blank', 'width=540,height=680'); if (!page) return; page.document.write(`<title>Payslip</title><main><h1>Payslip</h1><h2>${escapeHtml(entry.employee.user?.name)}</h2><p>${escapeHtml(entry.month)}/${escapeHtml(entry.year)} · ${escapeHtml(entry.payableDays)}/${escapeHtml(entry.workingDays)} payable days</p><table><tr><td>Gross pay</td><td>${escapeHtml(entry.grossPay)}</td></tr><tr><td>Deductions</td><td>${escapeHtml(entry.deductions)}</td></tr><tr><th>Net pay</th><th>${escapeHtml(entry.netPay)}</th></tr></table></main><style>body{font:16px Arial;padding:32px}main{max-width:440px;border:1px solid #222;padding:28px}table{width:100%;border-collapse:collapse}td,th{padding:10px;border-bottom:1px solid #ddd;text-align:left}</style>`); page.document.close(); page.print() }
