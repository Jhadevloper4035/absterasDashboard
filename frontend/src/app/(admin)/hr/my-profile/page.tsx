import PageMetaData from '@/components/PageTitle'
import PdfActionButton from '@/components/PdfActionButton'
import { apiFetch } from '@/helpers/api'
import { downloadIdCardPdf } from '@/helpers/idCard'
import { useAuthStore } from '@/store/authStore'
import type { EmployeeType } from '@/types/hr'
import { useEffect, useState } from 'react'
import { Alert, Badge, Card, CardBody, Table } from 'react-bootstrap'

const date = (value?: string) => (value ? new Date(value).toLocaleDateString() : '-')

const MyProfilePage = () => {
  const token = useAuthStore((state) => state.token)
  const [employee, setEmployee] = useState<EmployeeType>()
  const [error, setError] = useState('')
  useEffect(() => { apiFetch<{ data: EmployeeType }>('/hr/employees/me').then((response) => setEmployee(response.data)).catch((reason) => setError(reason instanceof Error ? reason.message : 'Unable to load your profile')) }, [])
  if (!employee) return <><PageMetaData title="My Profile & Documents" />{error ? <Alert variant="danger">{error}</Alert> : <div>Loading…</div>}</>

  const salary = employee.salary
  const monthlySalary = salary && salary.basic + salary.hra + (salary.allowances || []).reduce((total, allowance) => total + allowance.amount, 0)
  return <><PageMetaData title="My Profile & Documents" />{error && <Alert variant="danger">{error}</Alert>}<div className="row g-3 align-items-start"><div className="col-lg-4"><Card><CardBody className="text-center"><div className="mb-3">{employee.photo?.url ? <img src={employee.photo.url} alt={employee.user.name} className="rounded-circle border" style={{ width: 132, height: 132, objectFit: 'cover' }} /> : <div className="rounded-circle bg-primary-subtle text-primary d-inline-flex align-items-center justify-content-center fw-bold fs-1" style={{ width: 132, height: 132 }}>{employee.user.name.slice(0, 1)}</div>}</div><h3 className="mb-1">{employee.user.name}</h3><p className="text-muted mb-3">{employee.designation?.name || 'No job title'} · {employee.department?.name || 'No department'}</p><Badge bg={employee.status === 'active' ? 'success' : employee.status === 'terminated' ? 'danger' : 'secondary'}>{employee.status}</Badge><PdfActionButton className="d-block mx-auto mt-4" variant="outline-primary" action={() => downloadIdCardPdf(employee._id, token).catch((reason) => setError(reason instanceof Error ? reason.message : 'Unable to download ID card'))}>Download ID card</PdfActionButton></CardBody></Card></div><div className="col-lg-8"><Card className="mb-3"><CardBody><h4 className="card-title mb-1">Employment information</h4><p className="text-muted mb-3">Read-only information maintained by HR.</p><dl className="row mb-0 border-top pt-3"><dt className="col-sm-4 text-muted">Email</dt><dd className="col-sm-8">{employee.user.email || '-'}</dd><dt className="col-sm-4 text-muted">Mobile</dt><dd className="col-sm-8">{employee.user.phone || '-'}</dd><dt className="col-sm-4 text-muted">Employee ID</dt><dd className="col-sm-8">{employee._id.slice(-8).toUpperCase()}</dd><dt className="col-sm-4 text-muted">Department</dt><dd className="col-sm-8">{employee.department?.name || '-'}</dd><dt className="col-sm-4 text-muted">Job title</dt><dd className="col-sm-8">{employee.designation?.name || '-'}</dd><dt className="col-sm-4 text-muted">Joining date</dt><dd className="col-sm-8">{date(employee.joiningDate)}</dd><dt className="col-sm-4 text-muted">Date of birth</dt><dd className="col-sm-8">{date(employee.dateOfBirth)}</dd><dt className="col-sm-4 text-muted">Manager</dt><dd className="col-sm-8">{employee.manager?.name || '-'}</dd><dt className="col-sm-4 text-muted">Emergency contact</dt><dd className="col-sm-8">{employee.emergencyContact?.name ? `${employee.emergencyContact.name}${employee.emergencyContact.relation ? ` (${employee.emergencyContact.relation})` : ''}${employee.emergencyContact.phone ? ` · ${employee.emergencyContact.phone}` : ''}` : '-'}</dd></dl></CardBody></Card><Card className="mb-3"><CardBody><h4 className="card-title mb-3">Monthly salary</h4>{salary ? <div className="row g-3"><div className="col-sm-4"><small className="text-muted d-block">Monthly gross</small><strong className="fs-4">{monthlySalary}</strong></div><div className="col-sm-4"><small className="text-muted d-block">Basic salary</small><strong className="fs-4">{salary.basic}</strong></div><div className="col-sm-4"><small className="text-muted d-block">CTC</small><strong className="fs-4">{salary.ctc}</strong></div></div> : <p className="text-muted mb-0">No salary structure has been added.</p>}</CardBody></Card><Card><CardBody><div className="d-flex justify-content-between align-items-start flex-wrap gap-2 mb-3"><div><h4 className="card-title mb-1">Offer letter & documents</h4><p className="text-muted mb-0">Documents shared by HR, including your offer letter.</p></div><Badge bg="light" text="dark">{employee.documents.length} documents</Badge></div><Table responsive className="align-middle mb-0"><thead><tr><th>Type</th><th>File</th><th>Expiry</th></tr></thead><tbody>{employee.documents.map((document) => <tr key={document.key}><td><Badge bg="secondary">{document.type}</Badge></td><td>{document.url ? <a href={document.url} target="_blank" rel="noreferrer">{document.originalName || 'Open document'}</a> : document.originalName || 'Document'}</td><td>{date(document.expiresAt)}</td></tr>)}{!employee.documents.length && <tr><td colSpan={3} className="text-center text-muted py-4">No documents have been shared by HR yet.</td></tr>}</tbody></Table></CardBody></Card></div></div></>
}

export default MyProfilePage
